package buildingwebapp.run;

// This enum defines the possible location types where a run can take place in the fitness tracking application.

public enum Location {
    INDOOR,  // Represents runs done inside (treadmill, gym, indoor track, etc.)
    OUTDOOR  // Represents runs done outside (park, street, trail, etc.)
}